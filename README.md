# LayerZero × KelpDAO 事件剖析

單頁靜態簡報：以 HTML/CSS 呈現 LayerZero 與 KelpDAO 相關安全事件的技術整理與時間線。

## 本機預覽

在專案目錄開啟 `index.html`（或透過任意靜態伺服器提供根目錄）。

```bash
python3 -m http.server 8080
```

瀏覽器開啟 `http://localhost:8080` 即可。

## 授權

內容僅供研究與教育用途；請自行核對鏈上與官方資訊。
